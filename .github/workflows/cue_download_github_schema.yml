# Generated by CUE from ../cue/workflows_tool.cue; do not edit

name: Create PR with latest GitHub schema
"on":
  workflow_dispatch: {}
jobs:
  cue_fmt_check:
    name: CUE formatting check
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Checkout
        uses: actions/checkout@2541b1294d2704b0964813337f33b291d3f8596b
      - name: Setup CUE environment
        uses: cue-lang/setup-cue@143c2fe537047bf8c7ead6a30784ad1802e9d991
        with:
          version: v0.4.3
      - name: Download GitHub schema
        run: curl https://raw.githubusercontent.com/SchemaStore/schemastore/6fe4707b9d1c5d45cfc8d5b6d56968e65d2bdc38/src/schemas/json/github-workflow.json --output gh-workflows-schema.json
        working-directory: .github/cue/cue.mod/pkg/json.schemastore.org/github
      - name: Download GitHub schema
        run: 'cue import --force --package json --path ''#workflow:'' --outfile gh-workflows-schema.json jsonschema: ./cue.mod/pkg/json.schemastore.org/github/github-workflow.cue'
        working-directory: .github/cue/cue.mod/pkg/json.schemastore.org/github
      - name: Create pull request
        uses: peter-evans/create-pull-request@18f90432bedd2afd6a825469ffd38aa24712a91d
        with:
          add-paths: _(filename_gh_workflow_cue_schema)
          commit-message: Update...
          title: Update...
          delete-branch: true
          labels: cue,enhancement
      - name: Check commit is clean
        run: test -z "$(git status --porcelain)" || (git status; git diff; false)
permissions:
  contents: read
