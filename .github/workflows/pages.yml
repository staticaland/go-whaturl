# Generated by cue/workflows_tool.cue; do not edit
name: Deploy Hugo site to Pages
"on":
  push:
    branches:
      - main
    paths:
      - '**.org'
      - '**.md'
  workflow_dispatch: null
defaults:
  run:
    shell: bash
concurrency:
  group: pages
  cancel-in-progress: true
jobs:
  build:
    name: Build GitHub Pages with Hugo
    runs-on: ubuntu-latest
    env:
      HUGO_VERSION: 0.101.0
      HUGO_ENV: production
    steps:
      - name: Checkout
        uses: actions/checkout@2541b1294d2704b0964813337f33b291d3f8596b
        with:
          submodules: recursive
      - name: Convert README to Hugo compatible MD
        uses: docker://pandoc/core@sha256:65d98a5fb89867961479c16f4597134517923661aca23ef63843f6b6e623508b
        with:
          args: --from=org --to=gfm --output docs/content/index.md README.org
      - name: Install Hugo CLI
        run: |
          wget -O "${{ runner.temp }}/hugo.deb" "https://github.com/gohugoio/hugo/releases/download/v${HUGO_VERSION}/hugo_extended_${HUGO_VERSION}_Linux-64bit.deb" \
          && sudo dpkg -i "${{ runner.temp }}/hugo.deb"
      - id: pages
        name: Setup Pages
        uses: actions/configure-pages@2ce6d5eaa1f7b36a30c9b424cd73fab661857cfe
      - name: Build with Hugo
        run: |
          hugo \
          	--minify \
          	--baseURL "${{ steps.pages.outputs.base_url }}"
        working-directory: ./docs
      - name: Upload artifact
        uses: actions/upload-pages-artifact@a597aecd27af1cf14095ccaa29169358e3d91e28
        with:
          path: ./docs/public
  deploy:
    name: Deploy to GitHub Pages
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@c2379ec5e719a934ec613038d081879b58c9d7df
permissions:
  contents: read
  pages: write
  id-token: write
